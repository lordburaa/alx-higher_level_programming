#!/uar/bin/python3
def uppercase(str):
    g
    for i in str:
        if (ord(i) > 97 and ord(i) < 122):
            j = chr(ord(i) - 32)
            g += str(j)
        else:
            g += str(ord(i))
    return j
